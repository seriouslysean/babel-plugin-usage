{
  "name": "babel-7.8.0-usage",
  "version": "0.1.0",
  "description": "Learn how to use ECMAScript2020 features with Babel 7.8.0",
  "main": "index.js",
  "scripts": {
    "compile": "babel index.js --out-file dist/bundle.js --retain-lines",
    "start": "npm run compile && node dist/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sean Kennedy",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.5.0",
    "@babel/core": "^7.5.4",
    "@babel/preset-env": "^7.8.3"
  }
}
